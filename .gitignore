.DS_Store
*.log
.tooling-state.json
.tmp/

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Workflow state (ephemeral, per-repo)
.state/
!.state/.gitkeep

# Per-change workflow state (ephemeral)
spec/changes/*/state.yaml

.env
.mcp.json
.pricing-cache.json

# Cross-repo metrics runtime artifacts (per-machine, never gitted)
*.duckdb
/metrics-registry.yaml
.claude/metrics.jsonl
