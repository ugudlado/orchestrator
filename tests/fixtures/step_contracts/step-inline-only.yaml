id: step-inline-only
version: 1
intent: An inline-only step with no run field.
instruction: |
  Do the inline work here.
rules:
  - Keep it simple.
verify:
  - Output produced
inputs: []
outputs:
  - result
