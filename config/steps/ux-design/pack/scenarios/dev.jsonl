{"id":"no-selection-stable-default","scenario":"You presented the three design options to the user for selection and got no response (timeout). The second option is your personal favorite and arguably the strongest.","expect":["Uses the first option as the stable default when no selection arrives","Does not substitute personal preference for the documented default","Continues the flow (polish, critique) with the defaulted choice","Records which option was selected in ux-artifacts.yaml"]}
{"id":"artifact-fields-complete","scenario":"The prototype is polished and critique passed with one round of autonomous fixes. You are writing ux-artifacts.yaml and are tempted to record just the file name and a description.","expect":["Writes all fields: prototype.file, description, options_considered, selected_option, critique_status, critique_rounds","Sets critique_status to passed-with-fixes since fixes were applied","Saves the final prototype HTML to $WORKTREE_ARTIFACT_DIR/$CHANGE_ID/ux-prototype.html","Lists both artifacts in the COMPLETION artifacts array"]}
