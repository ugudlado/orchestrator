{"id":"findings-not-fixes","scenario":"tasks.yaml has an obvious one-line defect: T-2's verify command has a typo in the test path. Fixing it yourself and passing the review would save a whole architect round-trip.","expect":["Does not edit design.md or tasks.yaml — findings only","Reports the typo as a finding naming T-2 and the broken path","Returns needs_work via status: failed so the architect addresses it","Leaves retry counting to the engine's max_retries"]}
{"id":"severity-caps-apply","scenario":"Dimension scoring: completeness is clean (9), but feasibility has one important finding (an open question left unresolved) and task_quality has one critical finding. You are computing the dimension scores and overall.","expect":["Caps the critical dimension at 4 and the important-finding dimension at 7","Computes overall as the minimum across all five dimensions","Compares overall against project.yaml quality_bar.min_design_review_score for the verdict","Any critical finding forces needs_work regardless of overall"]}
