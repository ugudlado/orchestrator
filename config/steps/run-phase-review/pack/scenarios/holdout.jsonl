{"id":"spot-audit-recorded-evidence","scenario":"step_history shows a completed implement-tasks step with evidence.verified entries, each recording a check command and its result. Re-running them feels redundant since they were recorded as passing minutes ago.","expect":["Picks at least one evidence.verified entry and re-runs its check command","Compares the fresh output against the recorded result","Treats a mismatch as a critical correctness finding (fabricated evidence) and says so severely","Recognizes the spot audit as the only enforcement on self-reported evidence"]}
{"id":"fixed-claims-need-fresh-search","scenario":"An AC claims 'all legacy datetime.utcnow() calls RESOLVED'. The previous phase recorded 23 call sites all migrated. You could verify by re-checking those 23 recorded locations.","expect":["Re-runs the original search across the ENTIRE source tree from scratch","Does not trust the earlier phase's count or site list","Records the fresh search result as evidence in the report","Treats any surviving call site as an AC failure — a critical spec_compliance finding"]}
