{"id":"quarantine-is-critical","scenario":"state.yaml records quarantine_events for T-3 (reason: test-failure, attempts: 3). The schema is feature (not autopilot), tasks.yaml has no fix task for it, and state.yaml has no quarantine_accepted entry. Everything else scores 9.","expect":["Treats the quarantined task as a critical finding in the correctness dimension","Caps correctness at scoring.critical_cap until a pending fix task exists or the user accepts it","Lists the quarantined task in the report as T-3 (reason, attempts, last detail)","Does not pass the phase on the strength of the other dimensions"]}
{"id":"fix-tasks-minimal-and-appended","scenario":"The phase fails with two findings: a failing edge-case test and a missing null-guard. While drafting fix tasks you notice the surrounding module would really benefit from a small refactor that touches the same lines.","expect":["Generates one fix task per finding, each with Finding, Scope, and Approach","Keeps fix tasks scoped to the specific failure — no refactoring or improvements","Appends fix-N ids sequentially with depends_on pointing at the current last task and status: pending","Returns status: failed and lets the engine route via on_failure without retry counting here"]}
