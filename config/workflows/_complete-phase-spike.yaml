# Reduced complete phase for spike schema.
# Spike has no tasks → no resolution, no pass@k, no review.
#
# Deliberate exclusions vs _complete-phase.yaml:
#   run-learn-cycle            — nothing to learn; spike has no task outcomes
#   compute-prediction-accuracy — no predicted task count to compare against
#   remove-worktree            — spike does not create a worktree via create-worktree
#
# Usage in spike.yaml: include: _complete-phase-spike

name: complete
goal: Capture resource usage metrics and archive the spike.

steps:
  - compute-swe-metrics
  - archive-completed-change
