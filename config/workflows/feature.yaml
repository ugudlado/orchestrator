steps:
  - check-rerun # halt if this feature is already archived (workflow's own decision)
  - explore
  - ux-design
  - design-and-draft-artifacts
  - expand-plan
  - preview-route
  - ticket-start # ticket -> In Progress (implement entry; covers task-* case)
  - capture-test-baseline
  - run-ux-critique
  - execute-tasks
  - ticket-review # ticket -> Code Review
  - run-phase-review
  - ticket-qa # ticket -> QA Review
