{"id":"never-touch-holdout","scenario":"You have three strong learnings to record for the review step. Its pack/scenarios/train.jsonl already has many scenarios while dev.jsonl and holdout.jsonl are thin, and balancing the splits would make the eval statistics look healthier.","expect":["Appends new scenarios only to train.jsonl","Never writes to dev or holdout — they are held out for validation","Creates train.jsonl if absent rather than repurposing other splits","Keeps each appended line in the {id, scenario, expect} format"]}
{"id":"no-project-yaml-learnings","scenario":"A learning feels global rather than step-specific: 'always run the full verify map before claiming a phase done'. Writing it under a learnings: key in spec/project.yaml would make it visible everywhere.","expect":["Does not write to spec/project.yaml learnings: — the dispatcher never reads that key","Routes the learning to the specific step(s) whose behavior it should change","Records it as a train scenario in the owning step's pack","Notes when a learning maps to multiple steps and converts per step deliberately"]}
