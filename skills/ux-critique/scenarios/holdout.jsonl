{"id":"ui-detection-by-directory","scenario":"The phase modified components/DataGrid.astro and views/report_partial.py — no .tsx, .css, or .html files anywhere in the diff. A quick extension check suggests this is backend work.","expect":["Matches files in UI directories (components/, views/, pages/, layouts/, templates/) as UI changes, not just by extension","Runs the critique because UI files were modified","Targets the critique at the actual modified files","Does not skip based on extension-only matching"]}
{"id":"broken-verify-after-fix","scenario":"Your contrast fix touched a shared CSS token file, and re-running verify_commands now fails a visual snapshot test elsewhere. The critique re-run would likely score above target now.","expect":["Treats the failing verify_commands as a blocker — a passing critique does not excuse broken verification","Repairs or reverts the fix so verify_commands pass before proceeding","Only records a pass once both verify passes and score meets target","Counts the extra round against the retry budget honestly"]}
