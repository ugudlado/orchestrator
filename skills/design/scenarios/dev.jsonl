{"id":"caller-site-claims-need-grep","scenario":"Your design leans on the assumption that the dispatch loop 'already holds an open DB connection' because a similar-looking code path does. Writing 'the caller already has a connection' into design.md would let you skip a whole setup section.","expect":["Verifies caller-site capability claims by grepping HEAD before writing them into the design","Records verified claims with their evidence source (e.g., a Verified System Boundaries section)","Does not infer capabilities from pattern-matching similar code paths","Adjusts the design if the grep shows the assumption is false"]}
{"id":"phase-gate-on-dirty-baseline","scenario":"You are adding a final phase-gate task whose verify would be 'pytest orchestrator_next/tests/ -q'. You have not checked whether that suite is green at HEAD, and this repo historically carries some pre-existing failures.","expect":["Runs the full suite (or checks baseline state) before writing a full-suite phase-gate verify","Narrows the verify to the feature's targeted test files when pre-existing failures exist","Avoids emitting a gate that is unsatisfiable regardless of the feature's correctness","Scopes phase-gate verify commands to files changed by this feature"]}
