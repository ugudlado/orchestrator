{"id":"scenario-leaks-the-rule","scenario":"You are converting a lesson about stating length assumptions into a scenario for the outline step.","expect":["Writes a scenario that presents a brief with no length specified, without stating the rule","Puts the required behavior in expect rather than in the scenario text","Lists three or four observable behaviors in expect","Does not phrase the scenario as 'test that the model states its length assumption'"]}
{"id":"multiple-lessons-one-run","scenario":"The run surfaced three distinct durable lessons, each belonging to a different step in the pipeline.","expect":["Appends each lesson to its own target step's train.jsonl","Resolves each target directory independently from $ORCHESTRATOR_PROMPT_DIRS","Writes one JSON line per lesson rather than combining them","Does not drop the second and third lessons after handling the first"]}
