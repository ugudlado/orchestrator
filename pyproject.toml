[project]
name = "orchestrator"
version = "0.1.0"
description = "LLM-agnostic config-driven workflow engine"
authors = [{ name = "Mahesh", email = "mahesh288@gmail.com" }]
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]

# The wheel ships the engine ONLY — workflows come from orchestrator pack
# vendor (.orchestrator/) or a downloaded pack (~/.orchestrator/pack).
#
# Optional extras:
#   redis — Redis-backed ACP session store for cross-environment continuation
#   dev   — test/lint tooling
[project.optional-dependencies]
redis = ["redis>=5.0"]
dev = [
    "pytest>=8.0",
    "ruff>=0.4",
    "vulture>=2.16",
    "redis>=5.0",  # tests exercise the redis store path
]

[project.scripts]
orchestrator = "orchestrator_next.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["orchestrator_next"]

[tool.ruff]
line-length = 120
